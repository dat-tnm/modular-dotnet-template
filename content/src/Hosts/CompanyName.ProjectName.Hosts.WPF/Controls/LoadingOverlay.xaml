<UserControl x:Class="CompanyName.ProjectName.Hosts.WPF.Controls.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CompanyName.ProjectName.Hosts.WPF.Controls"
             xmlns:converters="clr-namespace:CompanyName.ProjectName.Hosts.WPF.Converters">
    
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Visibility="{Binding Converter={StaticResource BoolToVisibilityConverter}}">
        <Grid.Background>
            <SolidColorBrush Color="White" Opacity="0.8"/>
        </Grid.Background>
        
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <ProgressBar IsIndeterminate="True" 
                         Width="200" 
                         Height="4"
                         Foreground="#1976D2"/>
            <TextBlock Text="Loading..." 
                       Margin="0,16,0,0"
                       FontSize="16"
                       Foreground="#757575"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
